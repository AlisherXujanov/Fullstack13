import { globalReducer } from "./helpers"

const initialState = {
    text: "",
    range: 0,
    counter: 0,
    fontSize: 16,
    thisColor: "snow",
}

export {
    initialState,
    globalReducer
}